import { isSorted } from "../utils.js";
/**
 * Sorts an array using a miracle
 * Complexity: O(infinity)
 */
export const miracleSort = (arr, cmp) => {
    while (!isSorted(arr, cmp)) {
        // Waiting for a miracle
    }
    return arr;
};
//# sourceMappingURL=miracle-sort.js.map